v1.4.0, 2026-02-08
  - Added file attachments: attach, attachments, detach, download commands
  - Upload files to tasks (up to 25 MB) via Microsoft Graph API
  - Added --attach flag to 'new' command for attaching a file at creation time
  - Attachments displayed in 'show' command output (text and JSON)
  - Direct upload for files <= 3 MB, upload session for 3-25 MB
  - Added deep links: link, unlink, links commands
  - Attach URLs (Jira, GitHub, Slack, etc.) to tasks as linked resources
  - Added --link flag to 'new' command for attaching a link at creation time
  - Links appear as clickable "Open in {app}" in the Microsoft To Do app
  - Supports --json, --id, and -l flags

v1.2.0, 2026-02-05
  - Added -N/--note flag to 'new' command for creating tasks with notes

v1.1.0, 2026-02-05
  - Added note support: note, show-note, clear-note commands
  - Added sn, cn aliases for show-note and clear-note
  - Notes are now displayed in show command and JSON output
  - Added note field to Task model

v1.0.0, 2026-02-04
  - Fork from kiblee/tod0 with redesigned CLI
  - Removed interactive TUI, CLI-only
  - Added JSON output for all commands (--json flag)
  - Added --id flag for stable task identification
  - Added filters: --all, --completed, --due-today, --overdue, --important
  - Added uncomplete/uncomplete-step commands
  - Added inline step creation with -S flag
  - Renamed commands: lists (was ls), tasks (was lst), new-list (was newl)
  - Added command aliases: t, n, c, d
